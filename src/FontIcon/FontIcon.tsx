export const FontIcon = ({ items }) => {
  return (
    <div className="font-icon">
      <ul className="font-icon">
        {items.map((item) => (
          <li>
            <a href=""></a>
          </li>
        ))}
      </ul>
    </div>
  );
};
